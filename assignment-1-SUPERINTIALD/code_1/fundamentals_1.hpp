#ifndef READFILE_H__
#define READFILE_H__

#include <string>
#include <iostream>
#include <fstream>

using namespace std;

// TODO add detailed explanation on what the function should do
/*This function should copy the "old" array to "new" array then see if the new Value.
is bigger than values in the "new" array. Then reprint the "new" array back after inputting the new Value 
in to the correct array position.
*/
int addToArrayDesc(float sortedArray[], int numElements, float newValue);

#endif // READFILE_H__